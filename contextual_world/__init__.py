"""Contextual world package.

Import concrete runtime classes from their submodules, for example
`contextual_world.pipeline` or `contextual_world.runtime_adapter`.
Keeping this package initializer light lets `contextual_world.structure` be used
as the scene/map layer without importing the full world-action pipeline.
"""
